{
  "installCommand": "pip install -r requirements.txt && npm install",
  "buildCommand": "npx sass scss/custom-bootstrap.scss static/css/custom-bootstrap.min.css --style=compressed && python3 manage.py collectstatic --noinput && python3 manage.py migrate --noinput",
  "outputDirectory": "staticfiles",
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "agrogest/wsgi.py"
    }
  ]
}
