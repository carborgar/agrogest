{
  "buildCommand": "python manage.py collectstatic --noinput",
  "builds": [
    {
      "src": "agrogest/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb"
      }
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "agrogest/wsgi.py"
    }
  ]
}
